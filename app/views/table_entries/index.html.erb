<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Fancy Table Display</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #f4f6f8;
      color: #212529;
    }

    header {
      background: linear-gradient(135deg, #4e73df, #224abe);
      padding: 20px 40px;
      color: white;
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    footer {
      background: #343a40;
      color: #ced4da;
      text-align: center;
      padding: 12px;
      font-size: 14px;
      position: fixed;
      bottom: 0;
      width: 100%;
      letter-spacing: 0.5px;
    }

    .container {
      max-width: 800px;
      margin: 60px auto 100px auto;
      background: white;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    h2 {
      font-size: 22px;
      color: #343a40;
      border-bottom: 2px solid #dee2e6;
      padding-bottom: 8px;
      margin-bottom: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
    }

    th {
      background-color: #4e73df;
      color: white;
      text-align: left;
      padding: 12px;
      text-transform: uppercase;
      font-size: 13px;
      letter-spacing: 1px;
    }

    td {
      padding: 14px 12px;
      border-bottom: 1px solid #dee2e6;
      font-size: 16px;
    }

    tr:nth-child(even) {
      background-color: #f8f9fa;
    }

    td:last-child {
      font-weight: bold;
      color: #1b1e21;
    }

    .category {
      color: #495057;
    }
  </style>
</head>
<body>

  <header>
    Table Report System
  </header>

  <div class="container">
    <h2>Table 1 – Input Data</h2>
    <table>
      <thead>
        <tr><th>Index #</th><th>Value</th></tr>
      </thead>
      <tbody>
        <% @table_entries.each do |e| %>
          <tr>
            <td>A<%= e.row_number %></td>
            <td><%= e.value % 1 == 0 ? e.value.to_i : e.value %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <h2>Table 2 – Calculated Results</h2>
    <table>
      <thead>
        <tr><th>Category</th><th>Value</th></tr>
      </thead>
      <tbody>
        <tr>
          <td class="category">Alpha</td>
          <td><%= @alpha %></td>
        </tr>
        <tr>
          <td class="category">Beta</td>
          <td><%= @beta.is_a?(Numeric) ? @beta.to_i : @beta %></td>
        </tr>
        <tr>
          <td class="category">Charlie</td>
          <td><%= @charlie %></td>
        </tr>
      </tbody>
    </table>
  </div>

  <footer>
    Developed by <strong>Munirah Nazam</strong>
  </footer>

</body>
</html>
